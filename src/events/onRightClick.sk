on right click:
  set {_p} to player
  set {_targetName} to name of target
  if {_targetName} is {NPC::LISA::NAME}:
    lisa_rightClick()
  else:
    if block is set:
      set {_type} to block_nbt_get_tag(block, "TYPE")
      if {_type} is "GUI":
        gui_open({_p}, block)